# API Contracts

This feature does not involve external APIs or contract definitions.

The effect bypass toggle is implemented entirely within the browser using:
- Web Audio API (browser-native)
- Canvas API (browser-native)
- LocalStorage API (browser-native)

No REST APIs, GraphQL schemas, or external service contracts are required.

